N = int(input())
coords = []

for i in range(N):
    x, y = map(int, input().split())
    coords.append((x, y))

coords = sorted(coords, reverse = False)

for coord in coords:
    print(f"{coord[0]} {coord[1]}")